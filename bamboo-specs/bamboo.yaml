---
version: 2
plan:
  project-key: BAMLAB
  key: MAIN
  name: Main Plan
stages:
  - Build Stage:
      jobs:
        - Build Job
Build Job:
  tasks:
    - script:
        interpreter: SHELL
        scripts:
          - |
            echo "Bamboo Lab green build - bug bounty test instance"
            exit 0
---
version: 2
plan:
  key: BAMLAB-MAIN
plan-permissions:
  - users:
      - admin
      - planadmin_full
      - planadmin_nodeploy
    permissions:
      - view
      - edit
      - build
      - clone
      - admin
  - users:
      - rel_create
    permissions:
      - view
